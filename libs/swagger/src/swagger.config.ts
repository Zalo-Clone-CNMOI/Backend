import { SwagerConfig } from './swagger.interface';

export const SWAGGER_CONFIG: SwagerConfig = {
  title: 'API Documentation',
  description: 'API documentation for the application',
  version: '1.0',
  tags: ['template'],
};
